/**
 * Configuration du système de missions.
 * Modifier MAX_MISSION_POINTS pour ajuster la récompense maximale autorisée.
 */

/** Points d'escouade maximum attribuables par participant pour une mission. */
export const MAX_MISSION_POINTS = 60;

/** Points maximum autorisés pour une difficulté custom. */
export const MAX_CUSTOM_MISSION_POINTS = 100;
